# Deploy Skill
description: Tools and scripts to trigger deployments or build processes.

## Usage
When the user asks to "deploy" or "build for staging":
1. Run `npm run build` in the backend.
2. Run `npx expo export` in the mobile directory.
3. Validate artifacts.
